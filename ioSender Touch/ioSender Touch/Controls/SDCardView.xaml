<UserControl x:Class="ioSenderTouch.Controls.SDCardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:system="clr-namespace:System;assembly=mscorlib"
             xmlns:viewModels="clr-namespace:ioSenderTouch.ViewModels"
             
             d:DataContext="{d:DesignInstance Type=viewModels:SdCardViewModel, IsDesignTimeCreatable=True}"
             mc:Ignorable="d" 
             d:DesignHeight="515" d:DesignWidth="875">
    <UserControl.Resources>
        <system:String x:Uid="str_notConnection" x:Key="NoConnection">No connection.</system:String>
        <system:String x:Uid="str_uploading" x:Key="Uploading">Uploading...</system:String>
        <system:String x:Uid="str_downloading" x:Key="Downloading">Downloading {0}...</system:String>
        <system:String x:Uid="str_downloadAndRun" x:Key="DownloandRun">Download and run {0}?</system:String>
        <system:String x:Uid="str_transferDone" x:Key="TransferDone">Transfer done.</system:String>
        <system:String x:Uid="str_transferAborted" x:Key="TransferAborted">Transfer aborted.</system:String>
        <system:String x:Uid="str_transferring" x:Key="Transferring">Transferred {0} of {1} bytes...</system:String>
        <system:String x:Uid="str_deleteFile" x:Key="DeleteFile">Delete {0}?</system:String>
        <system:String x:Uid="str_illegalName" x:Key="IllegalName">File: {0}\n!,?,~ and SPACE is not supported in filenames, please rename.</system:String>
    </UserControl.Resources>
    <Grid>
        <DataGrid x:Uid="dgr_sdcard" x:Name="dgrSDCard" ItemsSource="{Binding  GrblSdCard.Files}" IsReadOnly="True" 
                  HorizontalAlignment="Left" Margin="30,30,0,0" VerticalAlignment="Top" Height="450" Width="380"
                  SelectionMode="Single" SelectionUnit="FullRow" AutoGenerateColumns="False"
                  CanUserReorderColumns="False" CanUserResizeColumns="False" CanUserResizeRows="False" CanUserSortColumns="False" CanUserAddRows="False"
                  SelectedItem="{Binding SelectedFile}"
                  ToolTip="Right click for menu, double click to run file.">
            <DataGrid.Columns>
                <DataGridTextColumn x:Uid="hdr_name" Header="Name" Binding="{Binding GrblSdCard.Name}" Width="*"/>
                <DataGridTextColumn x:Uid="hdr_size" Header="Size" Binding="{Binding GrblSdCard.Size}" Width="100"/>
                <!--
                <DataGridTextColumn Header="Remark" Binding="{Binding Invalid}" Width="*"/>
                -->
            </DataGrid.Columns>
            <DataGrid.ContextMenu>
                <ContextMenu x:Name="ctxMenu">
                    <MenuItem x:Uid="mnu_run" Header="Run"  Command="{Binding RunCommand}" />
                    <MenuItem x:Uid="mnu_downloadRun" Header="D_ownload and run"  Command="{Binding DownLoadRunCommand}" />
                    <MenuItem x:Uid="mnu_enableRewind" Header="Enable rewind" IsEnabled="{Binding Path=CanRewind}" IsChecked="{Binding Path=Rewind, Mode=TwoWay}" IsCheckable="True" />
                    <MenuItem x:Uid="mnu_viewAll" Header="View all" IsEnabled="{Binding Path=CanViewAll}" IsChecked="{Binding ViewAll, Mode=TwoWay}" IsCheckable="True" Command="{Binding ViewAllCommand}"/>
                    <MenuItem x:Uid="mnu_delete" Header="Delete" IsEnabled="{Binding Path=CanDelete}" Command="{Binding DeleteCommand}" />
                </ContextMenu>
            </DataGrid.ContextMenu>
            <DataGrid.InputBindings>
                <MouseBinding Gesture="LeftDoubleClick" Command="{Binding RunNowCommand }" />
            </DataGrid.InputBindings>
        </DataGrid>
        <Button x:Uid="btn_upload" Content="Upload" IsEnabled="{Binding Path=CanUpload}" HorizontalAlignment="Left" Margin="428,460,0,0" VerticalAlignment="Top" Width="75" Command="{Binding UploadCommand}"/>
    </Grid>
</UserControl>
